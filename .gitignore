# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# Local environment overrides (frontend/.env is committed for the challenge)
.env.local
.env.*.local
backend/.env

# Vite leaves these behind after a dev run
vite.config.ts.timestamp-*.mjs

# Submission material, handed in with the assignment rather than committed
NOTES.md
VIDEO_SCRIPT.md
_shots/
run-local.ps1

# submission artifacts
FINDINGS.pdf
